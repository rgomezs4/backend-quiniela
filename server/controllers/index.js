const users = require("./user");
const leagues = require("./league");
const teams = require("./team");
const stadiums = require("./stadium");
const participants = require("./participant");
const matches = require("./match");
const predictions = require("./prediction");

module.exports = {
    users,
    leagues,
    teams,
    stadiums,
    participants,
    matches,
    predictions
};
